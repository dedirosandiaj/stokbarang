import { pgTable, timestamp, integer, text, uuid } from 'drizzle-orm/pg-core';

const n = pgTable("users", { id: uuid("id").defaultRandom().primaryKey(), username: text("username").unique().notNull(), password: text("password").notNull(), role: text("role", { enum: ["admin", "kasir"] }).default("kasir").notNull(), createdAt: timestamp("created_at").defaultNow().notNull() }), s = pgTable("products", { id: uuid("id").defaultRandom().primaryKey(), brand: text("brand", { enum: ["nayyacorner", "inacookies"] }).notNull(), kodeBarang: text("kode_barang").notNull().unique(), namaBarang: text("nama_barang").notNull(), gramasi: integer("gramasi"), satuan: text("satuan"), harga: integer("harga").notNull(), stok: integer("stok").default(0).notNull(), createdAt: timestamp("created_at").defaultNow().notNull(), updatedAt: timestamp("updated_at").defaultNow().notNull() }), u = pgTable("sessions", { id: text("id").primaryKey(), userId: uuid("user_id").notNull().references(() => n.id), expiresAt: timestamp("expires_at", { withTimezone: true, mode: "date" }).notNull() }), d = Object.freeze(Object.defineProperty({ __proto__: null, products: s, sessions: u, users: n }, Symbol.toStringTag, { value: "Module" }));

export { d as a, s as p, u as s, n as u };
//# sourceMappingURL=schema-DxiP6MVG.mjs.map
